var vacationType=prompt('What type of trip would you like to go on, musical, tropical, or adventurous?');
console.log(vacationType); {

if(vacationType === "musical") {if (groupSize === '1' || groupSize === '2') {
   alert (result)
}
   prompt (groupSize == "2")
}
    
else if (vacationType === "tropical") {
    prompt (groupSize == "2")
}
else if (vacationTyoe === "adventerous") {
    prompt (groupSize == "2")
}
    
alert (result)
    
var groupSize = prompt ("How many are in your group?"
var travelType = "take a first class flight"
                        
var destination = prompt ()
let result;
    
var str1 = "Since you're a group of"
var str2 = " you should "
var str3 = " to ";
    
result = str1.concat (groupSize, str2, travelType, str3)

if (groupSize === "1") (return "take a first class flight")
    else if (groupSize === "3") {return "take a helicopter"}
    else if(groupSize >= (6)) {return " take a charter flight"}
